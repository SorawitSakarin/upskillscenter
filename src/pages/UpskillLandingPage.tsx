import React, { useState, ChangeEvent, FormEvent } from "react";

const UpskilLandingPage: React.FC = () => {
  return (
    <div
      className="flex flex-col p-10 gap-10"
      style={{ background: "#f4f4f4", minHeight: "100vh" }}
    >
      <h1>Upskill landing page</h1>
    </div>
  );
};

export default UpskilLandingPage;
